# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true

# Android settings
android.useAndroidX=true
android.enableJetifier=true

# Kotlin settings
kotlin.code.style=official
# Recommended JDK for building this project
# Use JDK 21 (Java 21) to match kotlinOptions.jvmTarget and compile settings
# Install and point your JAVA_HOME to a JDK 21 distribution.
requiredJdkVersion=21
